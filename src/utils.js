const square = (x)=>x*x;
const add = (x, y) => x+y;
const subtract = (x, y) => x-y;
export{ square, add, subtract as default}

//top 3 lines and below code have same meaning
//export const square = (x)=>x*x;
//export const add = (x, y) => x+y;